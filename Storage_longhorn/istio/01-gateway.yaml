apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: longhorn-gateway
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway   # usa el ingress por defecto de Istio
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "longhorn.kdvops.com"   # tu dominio
